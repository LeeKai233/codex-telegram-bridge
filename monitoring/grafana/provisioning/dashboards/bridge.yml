apiVersion: 1

providers:
  - name: codex-telegram-bridge
    orgId: 1
    folder: Codex Telegram Bridge
    folderUid: codex-telegram-bridge
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
